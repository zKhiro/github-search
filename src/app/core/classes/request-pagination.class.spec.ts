import { RequestPaginationClass } from './request-pagination.class';

describe('RequestPaginationClass', () => {
  it('should create an instance', () => {
    expect(new RequestPaginationClass()).toBeTruthy();
  });
});
