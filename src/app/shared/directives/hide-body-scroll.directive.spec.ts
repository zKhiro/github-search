import { HideBodyScrollDirective } from './hide-body-scroll.directive';


describe('HideBodyScrollDirective', () => {
  it('should create an instance', () => {
    const directive = new HideBodyScrollDirective();
    expect(directive).toBeTruthy();
  });
});
